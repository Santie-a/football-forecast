# Copy to .env (gitignored). Real values live per-host (and on the Pi, per the homelab skill).

# Path to the forecast store the dashboard reads (read-only).
# Local dev default; on the Pi this is the bind-mounted /data/forecasts.sqlite.
FORECAST_STORE=artifacts/forecasts/forecasts.sqlite
