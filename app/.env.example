# Copy to app/.env (gitignored). The dashboard reads the forecast store read-only.
# Local dev: path relative to where uvicorn runs (the project root).
# On the Pi: the bind-mounted store, e.g. /data/forecasts.sqlite (see deploy/DEPLOY.md).
FORECAST_STORE=artifacts/forecasts/forecasts.sqlite
